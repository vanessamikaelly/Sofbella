using Microsoft.AspNetCore.Mvc;
using SALAODEBELEZA.Models;

namespace SALAODEBELEZA.Controllers
{
    public class RelatorioController: ControllerBase
    {
        /*
        private static List<Agenda> agendas = new List<Agenda>();
        private static List<Caixa> financeiros = new List<Caixa>();
        private static List<Produto> produtos = new List<Produto>();
        [HttpGet("agenda")]
        public IActionResult GetRelatorioAgenda()
        {
            var relatorio = new Relatorio();
            var resultado = relatorio.GerarRelatorioAgenda(agendas);
            return Ok(resultado);
        }

        [HttpGet("financeiro")]
        public IActionResult GetRelatorioFinanceiro()
        {
            var relatorio = new Relatorio();
            var resultado = relatorio.GerarRelatorioFinanceiro(financeiros);
            return Ok(resultado);
        }

        [HttpGet("produtos")]
        public IActionResult GetRelatorioProdutos()
        {
            var relatorio = new Relatorio();
            var resultado = relatorio.GerarRelatorioProdutos(produtos);
            return Ok(resultado);
        }

        [HttpGet("geral")]
        public IActionResult GetRelatorioGeral()
        {
            var relatorio = new Relatorio();
            var resultado = relatorio.GerarRelatorioGeral(agendas, financeiros, produtos);
            return Ok(resultado);
        }
        */
    }
}
